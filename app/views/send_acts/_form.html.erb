<%= form_with(model: send_act) do |form| %>
  <% if send_act.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(send_act.errors.count, "error") %> prohibited this send_act from being saved:</h2>

      <ul>
        <% send_act.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data, style: "display: block" %>
    <%= form.text_area :data %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
